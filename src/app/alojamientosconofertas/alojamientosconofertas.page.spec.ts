import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AlojamientosconofertasPage } from './alojamientosconofertas.page';

describe('AlojamientosconofertasPage', () => {
  let component: AlojamientosconofertasPage;
  let fixture: ComponentFixture<AlojamientosconofertasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AlojamientosconofertasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
